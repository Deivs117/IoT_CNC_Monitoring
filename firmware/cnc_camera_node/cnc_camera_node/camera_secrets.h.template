// =============================================================================
// camera_secrets.h — Credenciales del nodo ESP32-CAM (NO committear)
// =============================================================================
//
// INSTRUCCIONES:
//   1. Copia este archivo como camera_secrets.h (si no existe ya).
//   2. Rellena los valores con tus credenciales reales.
//   3. Verifica que camera_secrets.h aparece en el .gitignore del repositorio.
//      Añade la línea:  firmware/cnc_camera_node/**/camera_secrets.h
//
// NUNCA commitas este archivo con credenciales reales.
//
// Para registrar el dispositivo cnc_camera_01 en Azure IoT Hub:
//   az iot hub device-identity create \
//     --hub-name <hub-name> --device-id cnc_camera_01
//
// Para obtener la clave primaria base64:
//   az iot hub device-identity show \
//     --hub-name <hub-name> --device-id cnc_camera_01 \
//     --query "authentication.symmetricKey.primaryKey" --output tsv
// =============================================================================

#pragma once

// ── WiFi ─────────────────────────────────────────────────────────────────────
#define WIFI_SSID   "TU_SSID"
#define WIFI_PASS   "TU_PASSWORD"

// ── Azure IoT Hub — conexión MQTTS directa (puerto 8883) ─────────────────────
// FQDN del IoT Hub, por ejemplo:
//   cnc-iot-hub-deiv-deployment.azure-devices.net
#define IOT_HUB_HOST         "TU_IOT_HUB_HOST"

// Clave primaria del dispositivo cnc_camera_01 en formato base64.
#define DEVICE_PRIMARY_KEY   "TU_DEVICE_PRIMARY_KEY_BASE64"

// Puerto MQTTS estándar de Azure IoT Hub
constexpr uint16_t MQTT_PORT = 8883;

// ── Certificado raíz de Azure IoT Hub ────────────────────────────────────────
// IMPORTANTE: solo actualizar si Microsoft cambia la cadena de confianza oficial.
// Fuente: https://learn.microsoft.com/en-us/azure/iot-hub/reference-iot-hub-tls-support
static const char* AZURE_ROOT_CA = R"EOF(
-----BEGIN CERTIFICATE-----
PEGAR_AQUI_EL_CERTIFICADO_RAIZ_OFICIAL_DE_AZURE
-----END CERTIFICATE-----
)EOF";
